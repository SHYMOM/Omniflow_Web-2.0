// Obsolete aggregator. Kept empty to satisfy compiler.
export class UltimateAggregator {
  async extractDirectStream() {
    return { success: false, sources: [], subtitles: [], headers: {} };
  }
}
